# M0 answer key

1. A — Python is readable and has a broad library ecosystem.
2. A — A package provides reusable code.
3. A — A function is named, reusable behavior.
4. A — A type hint communicates the expected value type.
5. A — A class groups related data and behavior.
6. A — A database stores data for later use.
7. A — A server receives requests and sends responses.
8. A — Validation prevents bad data from spreading.
9. A — Tests check expected behavior.
10. A — Logs help explain application behavior.
11. It returns the price including the tax rate.
12. `count` should be checked so it is not zero; other input validation may also be needed.
13. The program uses a variable before defining it or before it is in scope.
14. Application logic, persistent storage, and an HTTP server; test each boundary and then their composition.
15. Example: `get_portfolio_summary()` returns a validated portfolio summary object or response.
