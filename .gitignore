# ---------------------------------------------------------------------------
# Author-only. Never ships to participants who clone this repo.
# ---------------------------------------------------------------------------
.claude/
archive/
Instructor-Docs/
prompt_specs/
CLAUDE.md
*instructor-notes.md
CODE-ALONGS/
docs/superpowers

# ---------------------------------------------------------------------------
# Python. The two uv projects carry their own .gitignore as well; these are a
# safety net so nothing generated leaks in from a folder that lacks one.
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ipynb_checkpoints/

# State file the m9 state_resume.py toy writes next to itself
SLIDES-markdown/m9/pending_drafts.json

# ---------------------------------------------------------------------------
# Build output and tooling noise
# ---------------------------------------------------------------------------
node_modules/
dist/
build/
*.local
.DS_Store
.playwright-mcp/
OFFLINE-AI-Models/MODEL-DISTRIBUTION-PLAN.md
SLIDES-markdown/m7-code-cookbook.md
SLIDES-markdown/m7-concepts-reference.md

# Instructor-only answer keys (lab solutions)
answers/

# Instructor-only quiz solutions; keep participant question files trackable.
QUIZZES/**/answer-keys/
