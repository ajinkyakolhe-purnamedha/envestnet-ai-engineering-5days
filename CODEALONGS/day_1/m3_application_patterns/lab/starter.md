# Main lab · Chronos selection clinic

Timebox: 60–90 minutes. Choose patterns for the wealth-management, support,
and policy-document candidates in `decision_table.csv`, then add one candidate
from M1 use-case discovery.

For every row fill `candidate`, `workflow`, `data_boundary`, `pattern`,
`model_tier`, `why_not_simpler`, and `first_test`.

Implement only one tiny direct-call or prompted-extraction demonstration. Do
not implement RAG, fine-tuning, or an agent loop in this lab.
