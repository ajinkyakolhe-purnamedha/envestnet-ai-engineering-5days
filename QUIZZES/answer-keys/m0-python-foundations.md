# M0 answer key

1. B — Python combines readable application code with a broad ecosystem.
2. A — Engineering integrates capabilities into reliable products.
3. C — Gemini is the model provider.
4. B — Project metadata makes dependencies reproducible.
5. B — Configuration belongs outside source and missing configuration must be handled.
6. B — Type hints communicate an intended contract.
7. B — Functions create named, reusable, testable boundaries.
8. A — SQLite persists small structured data locally.
9. B — Health checks verify service reachability.
10. B — Logging, tests, and debugging support maintenance.
11. It returns `805.0`; zero shares raises `ValueError`.
12. B — The launch location does not expose the package on the import path.
13. The code reads the wrong variable name, so `key` is empty even when `GEMINI_API_KEY` is set.
14. Add the boundaries incrementally: first a model/data object, then storage operations, then the server endpoint. Test each boundary before composing them.
15. Example: `get_portfolio_summary()`; reject an unknown account or malformed date; test that a known fixture returns the expected holdings and totals.
